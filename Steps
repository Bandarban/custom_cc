Входные параметры:
    1. Размер входного слоя / входной слой.
    2. Скрытые слои
        а. Количество нейронов
        б. Функцию активации
    3. Выходной слой
    4. Количество эпох.
    5. Learning rate
    6.


Back propogation:
1. Считаю ошибку среднеквадратическую. E = (полученное значение - ожидаемое)**2
2. Считаю проиводную dE/dW
    Могу пересчитать веса: W(t+1) = w(t) - lr* dE/dW
3. Считаю производную dE/dB
   Могу пересчитать веса: B(t+1) = B(t) - lr* dE/dB
4. Считаю производную dE/du (на прошлый шаг)
   Могу повторить шаги 1-3




S(t) = D * T
a = dS(t)/dt
S(t+1) = S(t) + a

b = [2, 5]
b * w1 -> n1
b * w2 -> n2

[0.7, 0.7, 0.7]
